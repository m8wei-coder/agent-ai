# dependencies
/node_modules
/server/node_modules

# production
/build

# uploaded files (keep only the default sample PDF referenced by the server)
/server/uploads/*
!/server/uploads/hbs-lean-startup.pdf

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.claude

npm-debug.log*
yarn-debug.log*
yarn-error.log*
